server:
  port: 8123
  servlet:
    context-path: /api

spring:
  application:
    name: springboot-init
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    # ??????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # ????????????
      logic-delete-value: 1 # ?????????? 1?
      logic-not-delete-value: 0 # ?????????? 0?

knife4j:
  enable: true
  openapi:
    title: "????"
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.chengfu.usercenterapi.controller
